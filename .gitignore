.DS_Store
build/
build-*/
build_opt/
Testing/          # ctest artifacts when ctest is run from the source root
*.code-workspace
compile_commands.json
.cache/
site/
*.cgns
.clangd

# These files must be generated in the source directory so CEED's JIT compiler can find them.
src/swe/swe_ceed.h
src/tracer/tracer_ceed.h
